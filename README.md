# Angular with Gulp, Sass, Express, Mongoose and MongoDB Template

Simple project using Angular and MongoDB and Mongoose. Runs on an Express server.  Uses Gulp (3.x.x) and Webpack for preprocessing of Sass.
